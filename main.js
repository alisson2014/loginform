const emailInput = document.getElementById("email")
const passwordInput = document.getElementById("password")
const imgEmail = document.getElementById("mail")
const imgPassword = document.getElementById("lock")

const setFocus = () => {
  if (emailInput.focus) {
  }
}
